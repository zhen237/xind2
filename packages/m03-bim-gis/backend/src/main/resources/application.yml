server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/comm_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Admin@123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.comm.m03.entity

jwt:
  secret: xind2-bim-gis-secret-key-2026
  expire: 86400000

logging:
  level:
    com.comm.m03: DEBUG