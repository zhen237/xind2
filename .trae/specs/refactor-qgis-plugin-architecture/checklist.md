# Checklist

- [x] design_dock.py 不再包含内联的 BAND_CONFIGS 定义
- [x] design_dock.py 通过 `from design_engine.rules import BAND_CONFIGS` 引用频段配置
- [x] design_dock.py 的 `_generate_hex_grid()` 调用 `design_engine.hex_grid` 而非内联算法
- [x] design_dock.py 的 `_calc_heatmap_data()` 调用 `design_engine.coverage` 而非内联 Okumura-Hata
- [x] design_dock.py 的避让过滤使用 `design_engine.avoidance.AvoidanceChecker`
- [x] design_dock.py 的 `load_basemap()` 调用 `ui/basemap.py` 函数
- [x] design_dock.py 使用 `tools/station_tool.py` 的 `AddStationTool`
- [x] design_dock.py 的手动添加站点弹出 `ui/station_dialog.py` 的 `StationDialog`
- [x] hex_grid.py 的 rotation_deg 参数已实现（旋转+边界裁剪）
- [x] avoidance.py 的 MultiPolygon 坐标提取分别处理每个子多边形
- [x] data_sync.py 的 validSites 使用 avoidance_checker 统计
- [x] coverage_heatmap.py 的渲染器使用正确的 QGIS API（addClassRange）
- [x] coverage.py 的 calculate_coverage_rate() 接受 resolution_m 参数
- [x] 全部 13 个现有单元测试通过
- [ ] 为修复的 Bug 添加回归测试用例（待后续补充）
- [ ] QGIS 中插件加载无报错（需在 QGIS 环境中手动验证）
- [ ] 五步流程（底图→区域→参数→拓扑→导出）全部正常（需在 QGIS 环境中手动验证）
- [ ] 蜂窝拓扑生成结果与重构前一致（需在 QGIS 环境中手动验证）
- [ ] 覆盖热力图渲染与重构前一致（需在 QGIS 环境中手动验证）
- [ ] 手动添加站点弹出编辑对话框（需在 QGIS 环境中手动验证）
