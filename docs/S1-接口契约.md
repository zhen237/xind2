# S1 跨模块接口契约文档（M03 BIM-GIS ↔ QGIS 插件 ↔ S2/S3/S4/S5）

> 版本: v1.0.0（基于 2026-07-15 之后的代码，含 #185 幂等键、#182 实测来源、机房字段）
> 维护人: S1 模块负责人（高）
> 范围: S1（通信工程智能辅助设计）对外的所有接口契约，是跨客户端 / 服务端 / 下游赛题的唯一权威定义。

---

## 0. 文档定位与受众

S1 是整条流水线（**设计 → 审查(S3) → 施工指令(S4) → 施工监管(S5)**）的**源头**：设计师在 GIS 环境中完成基站 / 机房 / 管线的参数化智能设计，产出可被 S3 审查、S4 转化的结构化设计成果。

本文档定义三类契约：

| 契约 | 方向 | 状态 | 说明 |
|------|------|------|------|
| **契约 A** | QGIS 插件 → M03 后端 | ✅ 已落地（权威定义） | 客户端↔服务端，本次补全为权威 Schema |
| **契约 B** | M03 后端 → Python 拓扑引擎 | ✅ 已落地（S1 内部） | 供运维 / 排障参考 |
| **契约 C** | S1(M03) → S2/S3/S4/S5 下游 | 🟡 待联调（推荐契约） | 设计成果交移，跨赛题 API |

> **重要**：S2/S3/S4/S5 当前多为骨架，尚未实际调用 S1。契约 C 为**推荐契约**，下游应按此结构实现消费侧；实战联调时可能微调（风险项 R-1）。

---

## 1. 通用约定

### 1.1 Base URL 与端口

| 服务 | 端口 | Base Path | 说明 |
|------|------|-----------|------|
| M03 后端（Spring Boot） | `8083` | `/api/m03` | 固定端口（避开 WorkBuddy 自身 3455） |
| Python 拓扑引擎（FastAPI） | `9001` | `/generate`、`/health` | S1 内部依赖，不对外暴露 |
| M03 前端（Vite dev） | `9000` | `/modules/m03/` | 仅开发态 |

生产/内网访问示例：`http://{host}:8083/api/m03/design/upload`

### 1.2 统一响应信封 `Result<T>`

所有 JSON 响应均为以下结构（来自 `packages/shared/backend/.../Result.java`）：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { }
}
```

`code` 语义：

| code | 含义 | 客户端动作 |
|------|------|-----------|
| `200` | 成功 | 解析 `data` |
| `400` | 参数错误（含 Bean Validation 失败） | 不重试，提示用户修正入参 |
| `401` | 未认证 | 携带 / 刷新凭证后重试 |
| `403` | 禁止 | 终止，联系管理员 |
| `404` | 资源不存在 | 终止 |
| `500` | 服务器错误 / **限流** | 见 1.5 注意 |

**客户端判成功严格以 `code === 200` 为准**（QGIS `data_sync.py` 即如此判断）。

> ⚠️ **限流返回偏差（已知）**：限流耗尽时服务端返回 `code:500, message:"请求过于频繁，请稍后再试"`，**并非标准 429**。下游/客户端若对 5xx 做重试，会徒劳重试限流响应。契约建议服务端后续改为 429；在此之前，调用方应**识别 `message` 文案**而非仅看 code 来区分"真 5xx"与"限流 5xx"。

### 1.3 认证

| 端点族 | 当前鉴权 | 说明 |
|--------|----------|------|
| `/api/m03/design/**`、`/api/m03/project`、`/api/m03/region`、`/api/m03/model` | **内网免鉴权** | `application-shared.yml` 的 `security.permit-paths` 已放行，QGIS 裸 `requests` 可直调 |
| 其余（`/device` 等）及**跨赛题调用** | 需 JWT | `Authorization: Bearer <token>`（由 m01-auth 签发） |

> 公网暴露前必须将免鉴权接口改为 **API Key 或 IP 白名单**；当前内网放行仅适合局域网/本机。

### 1.4 幂等键与完整性校验

- **上传接口**要求请求体携带 `idempotencyKey`（QGIS 生成 UUID）。服务端查 `uk_scheme_idempotency` 唯一索引去重：重复键返回**已存在方案**（`dup=true`），不产生新方案、不翻倍写站点。
- **建议**（客户端应遵守、服务端后续将强制）：上传同时带 `X-Payload-Sha256` 请求头，值为请求体 JSON（按 key 排序、`ensure_ascii=False`）的 SHA256 十六进制串。当前服务端已记录该头但未强制校验，后续版本会校验并拒绝 `sha256` 不匹配的请求。

### 1.5 限流（RateLimitAspect，Guava 令牌桶）

| 接口 | 令牌桶（permits/sec） |
|------|----------------------|
| `/design/upload`、`/design/{id}/sites`（POST） | 10 |
| `/design/{id}/sites/measured` | 10 |
| `/design/{id}/sites/import`（CSV） | 5 |
| `/design/generate`、`/design/tasks/{id}/generate` | 5 |

超限返回 `code:500, message:"请求过于频繁，请稍后再试"`（见 1.2 注意）。生产默认开启（`rate-limit.enabled:true`），可通过配置关闭（测试环境用此开关）。

### 1.6 数据范围与校验规则（服务端强制，#185）

服务端 `DesignService.uploadDesignFull` 在写入前对每个站点做范围校验，越界站点**跳过写入**并计入 `skipped` / `errors`：

| 字段 | 合法范围 | 越界后果 |
|------|----------|----------|
| `longitude` | `[73, 135]`（中国陆地经度） | 跳过 |
| `latitude` | `[3, 54]`（中国陆地纬度） | 跳过 |
| `rsrp` | `[-140, 0]` dBm | 跳过 |

建议枚举（当前为自由字符串，契约建议下游按此收敛）：

- `siteType` ∈ `{MACRO, MICRO, INDOOR}`
- `scenario` ∈ `{URBAN, SUBURBAN, RURAL}`
- `roomType` ∈ `{汇聚机房, 核心机房, 接入机房}`（示例）
- `routeType` ∈ `{direct, manhattan}`（direct=直线路径，manhattan=曼哈顿路径）

---

## 2. 契约 A：QGIS 插件 → M03 后端 API

实现位置：QGIS 插件 `design_engine/data_sync.py`（类 `DataSync`）。

### 2.1 端点清单（QGIS 实际调用）

| Method | Path | 用途 | QGIS 方法 | 限流 |
|--------|------|------|-----------|------|
| `POST` | `/api/m03/design/upload` | 原子上传完整设计方案（方案+站点+机房） | `upload_design` | 10 |
| `GET` | `/api/m03/design/{schemeId}/sites` | 校验回环：拉回站点比对数量与 siteId 集合 | `_verify_upload` | — |
| `GET` | `/api/m03/design/{projectId}` | 下载设计方案 | `download_design` | — |
| `GET` | `/api/m03/project` | 拉取项目列表 | `fetch_projects` | — |

> 其余 design / project / model / region / device 端点由 M03 **前端**调用（见附录 8），QGIS 不直接调用。

### 2.2 上传请求体 `DesignData`（权威 Schema）

`POST /api/m03/design/upload`，`Content-Type: application/json`，建议带 `X-Payload-Sha256` 头。

```json
{
  "projectId": 1,
  "schemeName": "Base Station Design",
  "frequencyBand": "3.5GHz",
  "towerHeight": 35,
  "gridSize": "4x4",
  "totalSites": 3,
  "validSites": 3,
  "invalidSites": 0,
  "avgRsrp": -85.5,
  "routeType": "manhattan",
  "idempotencyKey": "3f1a...uuid",
  "sites": [
    {
      "siteId": "SITE-001",
      "siteName": "基站A",
      "longitude": 111.0,
      "latitude": 35.0,
      "towerHeight": 35,
      "siteType": "MACRO",
      "scenario": "URBAN",
      "rsrp": -82.3,
      "isValid": true,
      "invalidReason": ""
    }
  ],
  "machineRooms": [
    {
      "roomId": "ROOM-001",
      "name": "汇聚机房A",
      "longitude": 111.01,
      "latitude": 35.01,
      "roomType": "汇聚机房"
    }
  ],
  "deviceLayout": [ ]
}
```

**字段表（请求体）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `projectId` | Long | ✅ | 项目 ID（`@NotNull`） |
| `schemeName` | String | ✅ | 方案名称（`@NotBlank`） |
| `frequencyBand` | String | | 频段，如 `3.5GHz` |
| `towerHeight` | BigDecimal/number | | 塔高（米） |
| `gridSize` | String | | 网格大小，如 `4x4` |
| `totalSites` | Integer | | 客户端上报总站点数（用于计数对账） |
| `validSites` | Integer | | 有效站点数 |
| `invalidSites` | Integer | | 无效站点数 |
| `avgRsrp` | BigDecimal/number | | 平均 RSRP（dBm） |
| `routeType` | String | | `direct` / `manhattan`（QGIS 当前路由类型） |
| `idempotencyKey` | String | ✅（推荐） | UUID，服务端去重 |
| `sites` | List\<SiteData\> | ✅ | 站点列表（见下） |
| `machineRooms` | List\<Map\> | | 机房列表，元素：`roomId,name,longitude,latitude,roomType` |
| `deviceLayout` | List\<DevicePositionData\> | | 设备拓扑（来自 Python 拓扑引擎），可选 |

**`sites[].SiteData` 字段**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `siteId` | String | ✅ | 站点 ID（`@NotBlank`） |
| `siteName` | String | | 站点名称 |
| `longitude` | BigDecimal/number | | 经度（范围见 1.6） |
| `latitude` | BigDecimal/number | | 纬度（范围见 1.6） |
| `towerHeight` | BigDecimal/number | | 塔高（米） |
| `siteType` | String | | `MACRO`/`MICRO`/`INDOOR` |
| `scenario` | String | | `URBAN`/`SUBURBAN`/`RURAL` |
| `rsrp` | BigDecimal/number | | RSRP（dBm，范围见 1.6） |
| `isValid` | Boolean | | 是否通过避让校验 |
| `invalidReason` | String | | 无效原因 |

### 2.3 上传响应（`Result.data` = Map）

成功 `code:200`，`data` 为明细 Map（供 QGIS 校验回环）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `schemeId` | Long | 方案 ID（命中幂等键时为已存在方案 ID） |
| `dup` | Boolean | `true` 表示命中幂等键，返回已存在方案，未重复写 |
| `received` | int | 客户端上报 `totalSites` |
| `inserted` | int | 实际写入站点数（越界被 skip 的不计入） |
| `skipped` | int | 范围越界被跳过数 |
| `errors` | List\<{siteId,reason}\> | 被跳过站点的原因明细 |

> 校验回环（QGIS `_verify_upload`）：上传成功后 `GET /sites` 拉回，比对 `len(sites) == received` 且 `siteId` 集合一致，不一致仅告警、不影响上传结果。

### 2.4 客户端可靠性契约（QGIS `data_sync.py` 保证，#185）

| 保证 | 机制 |
|------|------|
| 不丢 | 上传前本地落盘 `~/.qgis_plugin_cache/upload_queue.json`，失败/中断保留，可重发 |
| 重试 | 指数退避 1s/3s/9s，最多 3 次；**仅** 5xx / 网络错误 / 超时重试，4xx 不重试 |
| 计数对账 | 发送前断言 `totalSites == len(sites)` 且 `validSites+invalidSites == totalSites` |
| 校验回环 | 上传后拉回 `GET /sites` 比对数量与 siteId 集合 |
| 幂等 | 每次生成 `idempotencyKey` UUID，服务端去重 |

---

## 3. 契约 B：M03 后端 → Python 拓扑引擎（S1 内部）

实现位置：`packages/m03-bim-gis/backend/.../design/client/TopologyEngineClient.java`。

- **Base URL**：`${topology.engine.url:http://localhost:9001}`，连接/读取超时 `${topology.engine.timeout-ms:5000}`。
- **调用**：`POST {baseUrl}/generate`，请求体为 **snake_case**：

| 字段 | 来源（GenerateRequest） | 必填 |
|------|------------------------|------|
| `project_id` | projectId | ✅ |
| `scheme_name` | schemeName | ✅ |
| `template_type` | templateType | |
| `center_longitude` | centerLongitude | ✅ |
| `center_latitude` | centerLatitude | ✅ |
| `coverage_radius` | coverageRadius | ✅ |
| `frequency_band` | frequencyBand | ✅ |
| `tower_height` | towerHeight | |
| `grid_size` | gridSize | |
| `antenna_height` | antennaHeight | |
| `sector_count` | sectorCount | |
| `scenario` | scenario | |

- **响应**：`TopologyGenerateResponse`（站点 + 完整设备拓扑）。
- **容错**：引擎不可达 / 超时 / 5xx → 返回 `null` → `DesignService` **回退本地算法**（不阻断主流程）。
- 拓扑引擎自身端点与 Schema 以其独立文档为准（不在本契约范围）。

---

## 4. 契约 C：S1(M03) → 下游 S2/S3/S4/S5 设计成果交移

S1 是流水线源头，下游按"**拉模式**"消费 S1 只读端点，或（未来）由 S1 在方案定稿后推送。本契约定义**下游应消费的数据视图**与**各下游职责**。

### 4.1 下游可消费的数据视图（推荐）

| 数据 | 获取方式 | 返回类型 |
|------|----------|----------|
| 设计方案 | `GET /api/m03/design/{projectId}` | `DesignScheme` |
| 站点清单 | `GET /api/m03/design/{schemeId}/sites` | `Site[]` |
| GeoJSON（几何/覆盖） | `GET /api/m03/design/{projectId}/geojson` | String(GeoJSON) |
| 设备拓扑 | `m03_device` 表 / 上传 `deviceLayout` | `Device[]` |
| 机房位置 | `DesignScheme.roomLongitude/roomLatitude/roomName` + 上传 `machineRooms` | — |

### 4.2 各下游职责与所需数据

| 下游 | 赛题 | 消费 S1 数据 | 产出 |
|------|------|--------------|------|
| **S2** | 多源异构工程数据融合（CAD） | GeoJSON / 站点几何 + 机房位置 | 与 CAD 图纸融合后的几何（可回填 M03 或独立存储） |
| **S3** | 基于行业标准的设计智能审查 | `DesignScheme` + `Site[]`（含 `rsrp`、`rsrpSource`、`isValid`） | 审查结论（问题清单 / 合规率 / 修订建议） |
| **S4** | 设计成果向施工指令自动转化 | `DesignScheme` + `Site[]` + `machineRooms` + `deviceLayout` | 物料清单(BOM) / 施工指令（对应 QGIS `bom_generator` 逻辑可迁移至此） |
| **S5** | 施工过程智能监管 | `DesignScheme` + `Site[]` + 设备位置（作为"设计基线"） | 与实际施工进度比对的监管结论 |

### 4.3 推荐的跨赛题调用契约（待联调落地）

| 项 | 约定 |
|----|------|
| 鉴权 | 下游持 m01-auth 签发的 JWT，`Authorization: Bearer <token>` |
| 关联键 | 下游以 `schemeId` + `projectId` 作为跨赛题关联键 |
| 数据格式 | JSON（UTF-8），时间字段 ISO-8601 |
| 版本 | 现状路径无 `/v1` 前缀；**未来统一加 `/v1`**，本文档以当前路径为准 |
| 推送（可选） | 方案定稿后可经消息队列 / Webhook 通知下游（当前未实现） |

> **风险 R-1**：S2–S5 后端当前为空壳，跨赛题联调未实战，接口契约可能需返工。各赛题有实质内容后统一联调。

---

## 4.4 FTTH 真实数据集视图（补充，S1 设计成果的 FTTH 维度）

> 2026-08-07 增补：FTTH 真实数据（摩洛哥 Plan de récolement 样本 + 模版）已由 QGIS 插件 ftth 导出链生成，并通过只读端点暴露，下游可按此消费 **FTTH 维度** 的设计成果（与 4.1 的基站 DesignScheme 互补）。

### 端点

| Method | Path | 说明 | 鉴权 |
|--------|------|------|------|
| `GET` | `/api/m03/ftth` | 数据集列表（`datasets/index.json`：tag + 来源 + 8 图层 summary） | 内网免鉴权 |
| `GET` | `/api/m03/ftth/{tag}` | 三件套聚合 `{data, validation, plan}` | 内网免鉴权 |
| `GET` | `/api/m03/ftth/{tag}/data` | 8 图层设计成果（`ftth-data.json`） | 内网免鉴权 |
| `GET` | `/api/m03/ftth/{tag}/validation` | 38 规则自检结果 + anomalies + suggestions（`ftth-validation.json`） | 内网免鉴权 |
| `GET` | `/api/m03/ftth/{tag}/plan` | 规划成果（需求点/规划箱体/规划缆/对比，`ftth-plan.json`） | 内网免鉴权 |

- `tag` 正则：`^[A-Za-z0-9._-]+$`；服务端做目录穿越防护。
- 数据目录由 `ftth.data-dir` 配置（默认 `../frontend/public/datasets`，可用环境变量 `FTTH_DATA_DIR` 覆盖）。
- 关联键：本视图以 `tag`（数据集标识，含 `pm_code`）作为 FTTH 维度的关联键；与基站维度（`schemeId`+`projectId`）是两套独立关联体系，下游按需取用。

### 数据字段（下游消费要点）

`ftth-data.json` 顶层：`source` / `generated_at` / `summary` / `pm_list` / `boites` / `cables` / `sites`。

| 实体 | 关键字段 | 下游用途 |
|------|----------|----------|
| `boites`（光交箱/分纤箱） | `code, type, capacite_fo`（光纤容量）, `fonction`, `pm`, `logements`（覆盖户数）, `x, y, adresse` | **S4 BOM**：箱体数量/容量/型号；**S5**：设计基线几何 |
| `cables`（光缆） | `code, nom, origine, extremite, from, to, capacite_fo`（芯数）, `longueur`（长度）, `type_cable, pm` | **S4 BOM**：缆长/芯数/类型 → 物料清单；**S3**：连通性（`from`/`to` 连接 boites） |
| `sites`（站点/建筑） | `code, type, x, y, adresse` | **S2**：几何；**S5**：基线 |
| `pm_list` / `summary` | PM/SRO 根节点 + 8 图层计数（IMB/SITE/BOITE/CABLE/PTECH/INFRA/ZNRO/ZPM） | 概览/对账 |
| `validation`（38 规则） | `groups/rules/anomalies/suggestions` | **S3**：直接复用行业标准审查结论；`suggestions` 给修复指引 |
| `plan` | `params/demand_points/planned_boites/planned_cables/comparison` | **S4/S5**：规划 vs 现状对比 |

### 各下游取 FTTH 数据的推荐方式

| 下游 | 端点 | 消费字段 |
|------|------|----------|
| **S2** CAD 融合 | `GET /ftth/{tag}/data` | `boites/cables/sites` 的 `x,y` 几何（与 CAD 图纸融合） |
| **S3** 审查 | `GET /ftth/{tag}` 或 `/validation` | `validation` 38 规则 + `cables.from/to` 连通性 + 8 图层要素 |
| **S4** BOM | `GET /ftth/{tag}` 或 `/data` | `cables{longueur,capacite_fo,type_cable}` + `boites{capacite_fo,type,logements}` |
| **S5** 监管 | `GET /ftth/{tag}` 或 `/data` | `boites/cables/sites` 几何 + `plan` 路由（作为设计基线比对） |

> 注：纤芯级熔接计划、CAD/DWG 出图按项目决策暂未纳入本视图。

## 4.5 其他可对外数据（补充，堵住下游联调断点）

> 2026-08-07 增补。4.1/4.4 已覆盖基站 DesignScheme 与 FTTH 维度，但 S1(M03) 实际还有 4 类数据/能力**未在前两节列出**，其中两项是下游联调的硬断点。本节补齐，下游负责人读完 4.1–4.5 即可拿到 S1 全部对外资产。
> 下列端点完整路径表亦见 [附录 8](#8-附录m03-完整-rest-端点表供前端--下游参考)，本节只讲"下游怎么取、取来干嘛"。

### 4.5.1 🔴 联调硬断点（必须先读）

**① 关联键 `projectId` 从哪来** —— 4.1/4.2 反复用 `schemeId + projectId` 做跨赛题关联键，但此前未告知下游上哪拉 `projectId` 列表。

| Method | Path | 说明 | 鉴权 |
|--------|------|------|------|
| `GET` | `/api/m03/project` | 项目列表（**首屏第一个要调的端点**，拿 `projectId`） | 内网免鉴权 |
| `GET` | `/api/m03/project/{id}` | 单个项目详情 | 内网免鉴权 |

> 下游标准开工顺序：`GET /project` → 取 `projectId` → `GET /design/{projectId}` 拿 DesignScheme → `GET /design/{schemeId}/sites` 拿站点。

**② 设备拓扑只读端点** —— S5 监管要"设备位置基线"，4.1 只说"读 `m03_device` 表"，但正式取数应走以下端点（**不要直连数据库**）：

| Method | Path | 说明 | 下游用途 |
|--------|------|------|----------|
| `GET` | `/api/m03/device` | 设备全量列表 | S5 设备基线 |
| `GET` | `/api/m03/device/{id}` | 单设备 | 详情 |
| `GET` | `/api/m03/device/project/{projectId}` | 按项目过滤设备 | S5 按工程拉设备 |
| `GET` | `/api/m03/device/station/{stationCode}` | 按站点过滤设备 | S2/S5 按站点拉设备 |
| `GET` | `/api/m03/device/type/{deviceType}` | 按设备类型过滤（如 RRU/BBU/天线） | S4 物料归类 |

### 4.5.2 🟡 能力型数据（建议下游复用，非必须）

| Method | Path | 说明 | 可被谁复用 |
|--------|------|------|------------|
| `POST` | `/api/m03/llm/parse-design-params` | 自然语言/图纸 → 结构化设计参数 | **S3** 审查入参预处理、**S4** 需求解析 |
| `POST` | `/api/m03/llm/generate-report` | 设计方案 → Markdown 评审/交付报告 | **S3** 出审查报告、**S5** 出监管周报 |
| `GET` | `/api/m03/model` | 3D BIM 模型列表 | **S2** CAD 融合拉模型几何 |
| `GET` | `/api/m03/model/{id}` | 单模型 | — |
| `GET` | `/api/m03/model/type/{modelType}` | 按模型类型过滤 | S2 |
| `GET` | `/api/m03/region` | 区域层级列表 | 下游空间归属/过滤 |
| `GET` | `/api/m03/region/{id}` | 单区域 | — |
| `GET` | `/api/m03/region/parent/{parentCode}` | 按父区域查子区域 | 空间钻取 |
| `GET` | `/api/m03/design/templates` | 设计参数化模板列表 | 下游复用 S1 标准方案骨架 |
| `GET` | `/api/m03/design/templates/{templateId}` | 单模板 | — |

> LLM 端点为同步推理，下游调用须带超时与降级（S1 不保证 LLM 可用性）；模型/区域/模板为只读静态资源，可缓存。

### 4.5.3 🟢 离线交付物（非 API，QGIS 导出）

FTTH 导出链（`export_runner.export_from_dbf`）除 4.4 的 3 个 JSON 外，还产出 **4 类 xlsx**（需 `openpyxl`，由 QGIS 插件或离线脚本生成，落 `docs/真实数据/` 或导出目录），下游可按文件取用：

| 文件 | 内容 | 下游用途 |
|------|------|----------|
| `Plans_de_Boite.xlsx` | 箱体平面布置（位置/容量/覆盖户数） | **S4** BOM 箱体清单参考 |
| `Routes_Optiques.xlsx` | 光路由明细（缆长/芯数/起止箱体） | **S4** 缆材清单、**S3** 连通性核对 |
| `Plan_de_Baie_{PM}.xlsx` | 机架布置 / 端子层（按 PM 分册） | **S4** 端子物料、**S5** 成端基线 |
| `Synoptique_{PM}.xlsx` | 综合示意图（按 PM 分册） | **S3/S5** 拓扑概览 |

> 纤芯级熔接明细（F5/Q8）仍不在 8 图层，故上述 xlsx 亦不含纤芯级数据——按项目决策继续搁置。

### 4.5.4 下游取数速查（一句话总结）

| 下游 | 必读 | 可选复用 |
|------|------|----------|
| **S2** CAD 融合 | 4.1 GeoJSON + 4.4 FTTH 几何 | 4.5.2 Model/Region |
| **S3** 审查 | 4.1 RSRP + 4.4 validation + 4.5.1 ②设备 | 4.5.2 LLM/generate-report |
| **S4** BOM | 4.1 DesignScheme + 4.4 cables/boites + 4.5.3 xlsx | 4.5.2 LLM/parse、Device type |
| **S5** 监管 | 4.1 Site/DesignScheme + 4.4 plan + 4.5.1 ②设备 | 4.5.2 LLM/generate-report、Region |

## 5. 数据字典（核心实体字段）

### DesignScheme（响应 / DB：`m03_design_scheme`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 方案 ID（主键） |
| `projectId` | Long | 所属项目 ID |
| `schemeName` | String | 方案名称 |
| `frequencyBand` | String | 频段 |
| `towerHeight` | BigDecimal | 塔高（米） |
| `gridSize` | String | 网格大小 |
| `totalSites` / `validSites` / `invalidSites` | Integer | 站点计数 |
| `avgRsrp` | BigDecimal | 平均 RSRP（dBm） |
| `roomLongitude` / `roomLatitude` / `roomName` | BigDecimal/String | 机房位置（QGIS 同步） |
| `routeType` | String | `direct` / `manhattan` |
| `idempotencyKey` | String | 上传幂等键（唯一索引） |
| `createTime` / `updateTime` | LocalDateTime | 时间戳 |

### Site（响应 / DB：`m03_site`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 主键 |
| `schemeId` | Long | 所属方案 ID |
| `siteId` | String | 站点 ID |
| `siteName` | String | 站点名称 |
| `longitude` / `latitude` | BigDecimal(12,6) | 经纬度 |
| `towerHeight` | BigDecimal | 塔高 |
| `siteType` / `scenario` | String | 类型 / 场景 |
| `rsrp` | BigDecimal | RSRP（dBm） |
| `rsrpSource` | String | `simulated`（模型仿真，Okumura-Hata）/ `measured`（实测/现场勘测） |
| `isValid` | Integer(0/1) | 是否有效 |
| `invalidReason` | String | 无效原因 |

> **`rsrpSource` 语义（#182）**：前端覆盖分析据此判断用真值（实测）还是估算值（仿真）。实测站点经 `/sites/measured` 或 `/sites/import`(CSV) 导入，强制 `rsrpSource=measured`。

---

## 6. 错误码与异常处理约定

- **Bean Validation 失败**：返回 `code:400`，`message` 为字段校验信息（如"项目ID不能为空"）。
- **资源不存在**：`code:404`，`message` 如"未找到设计方案"。
- **范围越界（#185）**：**不**返回错误，而是跳过该站点并在 upload 响应的 `skipped` / `errors` 中体现（保证"部分成功"而非整体失败）。
- **限流**：`code:500, message:"请求过于频繁，请稍后再试"`（见 1.2 注意）。
- **下游消费建议**：联调取数时，**优先用 `code` 判断成功**；对 `code:500` 需进一步比对 `message` 区分限流与真错误。

---

## 7. 版本与变更管理

- 本文档随 M03 后端代码演进；任何 endpoint / Schema 变更须同步更新本文档并 bump 版本号。
- **已知待修复偏差**（研发债，下游实现时需知晓）：

| 偏差 | 现状 | 建议 |
|------|------|------|
| 限流返回码 | `500` 而非 `429` | 改为 `429` + `Retry-After` |
| 路径版本 | 无 `/v1` 前缀 | 统一加 `/v1` |
| `X-Payload-Sha256` | 服务端未强制校验 | 后续强制校验，拒绝不匹配请求 |
| `siteType`/`scenario` | 自由字符串 | 收敛为枚举常量 |
| 跨赛题鉴权 | 内网免鉴权 | 公网改 API Key / IP 白名单 |

---

## 8. 附录：M03 完整 REST 端点表（供前端 / 下游参考）

### Design（`/api/m03/design`）
| Method | Path | 说明 | 限流 |
|--------|------|------|------|
| POST | `/upload` | 原子上传方案+站点+机房（幂等） | 10 |
| GET | `/{projectId}` | 下载设计方案 | — |
| GET | `/{projectId}/geojson` | GeoJSON | — |
| POST | `/{schemeId}/sites` | 上传单站点 | 10 |
| GET | `/{schemeId}/sites` | 拉回站点（校验回环） | — |
| POST | `/{schemeId}/sites/measured` | 导入实测站点(JSON) | 10 |
| POST | `/{schemeId}/sites/import` | 导入实测站点(CSV, multipart) | 5 |
| DELETE | `/{schemeId}` | 删除方案 | — |
| POST | `/generate` | 参数化生成设计（调拓扑引擎） | 5 |
| GET | `/templates` | 模板列表 | — |
| GET | `/templates/{id}` | 模板详情 | — |
| POST | `/templates` | 创建模板 | — |
| PUT | `/templates/{id}` | 更新模板 | — |
| DELETE | `/templates/{id}` | 删除模板 | — |
| POST | `/tasks` | 创建设计任务 | — |
| GET | `/tasks` | 任务列表（可按 status 过滤） | — |
| GET | `/tasks/{id}` | 任务详情 | — |
| PUT | `/tasks/{id}/status` | 更新任务状态 | — |
| DELETE | `/tasks/{id}` | 删除任务 | — |
| POST | `/tasks/{id}/generate` | 执行任务生成设计 | — |

### Project / Model / Region / Device / Health
| Method | Path | 说明 |
|--------|------|------|
| GET/POST/PUT/DELETE | `/api/m03/project[/...]` | 项目管理（QGIS `fetch_projects` 用 GET 列表） |
| GET/POST/PUT/DELETE | `/api/m03/model[/...]` | 3D 模型管理（含 `/type/{type}`） |
| GET/POST/PUT/DELETE | `/api/m03/region[/...]` | 区域管理（含 `/parent/{code}`） |
| GET/POST/PUT/DELETE | `/api/m03/device[/...]` | 设备管理（含 `/project/{id}`、`/station/{code}`、`/type/{type}`） |
| GET | `/api/m03/health` | 健康检查（返回 `{status:"UP", service, timestamp}`） |

> 注：Project/Model/Region 端点当前**内网免鉴权**（见 1.3）；**Device 端点仍需 JWT**（见 1.3，未列入 permit-paths）。公网暴露前需收紧免鉴权端点。

## 9. 公共使用的数据（跨模块共享基础设施）

> 2026-08-07 增补。前 8 节聚焦 S1(M03) 自身接口；但下游 S2–S5 在消费 S1 之前/之中，还会用到一套**所有模块公共依赖**的共享数据。本节列出这些"公共使用的数据"及其来源（m01-auth 认证、screen 大屏聚合、portal/shared 公共层），供下游负责人一次性了解跨模块依赖。

### 9.1 🔑 m01-auth：统一认证（所有模块的鉴权前置）

S1 契约 C 1.3 提到的 `JWT` 即由 m01-auth 签发。**下游调 S1 任何需鉴权端点前，必须先到 m01-auth 拿 token**。

| Method | Path | 说明 | 响应 `Result.data` |
|--------|------|------|---------------------|
| `POST` | `/api/m01/auth/login` | 登录换取令牌 | `{token, userId, username, realName}` |
| `GET` | `/api/m01/auth/validate` | 校验令牌（`Authorization: Bearer <token>`） | `{userId, username}` |
| `GET` | `/api/m01/user` | 用户列表（含禁用状态 `status`） | `User[]` |
| `GET` | `/api/m01/user/{id}` | 单用户 | `User` |
| `GET` | `/api/m01/menu/user` | 当前用户菜单 + 权限点 | `Menu[]` |

- `login` 入参：`{username, password}`（`LoginRequest`，服务端校验）。
- token 由 `JwtUtils.generateToken(userId, username)` 签发；下游将其放入 `Authorization: Bearer <token>` 请求头。
- `validate` 失败返回 `401 unauthorized`（见 `Result.unauthorized`）。
- **当前 S1 的 FTTH / Project / Model / Region 端点内网免鉴权**（前置 `permit-paths`）；**Device 端点仍需 JWT**（见 1.3，未列入 permit-paths）。公网环境应统一改为携带 m01-auth 签发的 JWT。下游开发时建议**默认走鉴权**，避免联调切换时返工。

### 9.2 📊 screen：大屏公共聚合数据

`screen` 模块提供跨模块只读聚合视图，任何下游模块的大屏/驾驶舱均可直接嵌入（不依赖 S1，但数据常含 S1 设计产出）：

| Method | Path | 说明 |
|--------|------|------|
| `GET` | `/api/screen/ops-overview` | 运营总览（项目数 / 设计进度 / 设备健康汇总） |
| `GET` | `/api/screen/alert-monitor` | 告警监控（异常设计 / 审查不达标项） |
| `GET` | `/api/screen/project-progress` | 项目进度（按赛题 S1–S5 拆分） |
| `GET` | `/api/screen/energy-analysis` | 能耗分析 |
| `GET` | `/api/screen/station-gis` | 站点 GIS 分布（可叠加 S1 设计几何） |

> screen 数据为聚合只读，下游如需自定义维度，建议直接消费上游模块端点（如 S1 的 `/design`、`/ftth`）自行聚合，而非改 screen。

### 9.3 🧩 m06-portal / shared：公共层说明（无独立数据 API）

| 模块 | 性质 | 下游须知 |
|------|------|----------|
| `m06-portal` | 前端门户（模块目录 / 导航 / 单点入口） | 无独立 REST 数据端点；提供各赛题模块注册与跳转。下游模块的"入口"由 portal 统一编排，但数据仍走各模块自身 API（如 S1 的 `/api/m03/*`）。 |
| `shared` | 公共后端库（非独立服务） | 提供 `Result<T>` 统一响应信封、`JwtUtils`、公共缓存/枚举等。**不暴露数据端点**，但所有模块（含 S1）的响应格式、`Result.success/error/unauthorized` 均来自此库——下游消费任何模块响应时，按 `shared` 的 `Result` 结构解析即可（详见 1.2）。 |

### 9.4 公共数据依赖速查（下游集成顺序）

```
1. POST /api/m01/auth/login  → 拿到 token
2. 后续所有请求头带 Authorization: Bearer <token>
3. 按业务调对应模块端点：
   - S1 设计成果   → /api/m03/design、/api/m03/ftth（详见 4.1 / 4.4 / 4.5）
   - 项目/区域/设备 → /api/m03/project、/api/m03/region、/api/m03/device（详见 4.5.1）
   - 大屏展示      → /api/screen/*（详见 9.2）
4. 解析响应统一用 shared 的 Result<T> 信封（详见 1.2）
```

> 注：除 m01-auth 外，其余共享端点（screen/project/region/device）当前内网免鉴权；统一收紧到 JWT 鉴权见各模块 `security.permit-paths` 配置与 1.3。
