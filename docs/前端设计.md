# 前端设计

## 一、技术栈概述

前端采用Vue3生态系统构建，使用Vite作为构建工具，实现组件化、模块化开发。

### 1.1 核心技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4+ | 前端框架 |
| Vite | 5.x | 构建工具 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Element Plus | 2.5+ | UI组件库 |
| Cesium | ^1.141.0 | 3D地图 |
| Axios | ^1.6.8 | HTTP客户端 |
| ECharts | ^6.1.0 | 图表展示 |

## 二、项目结构

```
m06-portal/
├── public/                    # 静态资源
│   ├── models/               # 3D模型文件
│   └── images/              # 图片资源
├── src/
│   ├── assets/              # 资源文件
│   ├── components/          # 公共组件
│   │   ├── CesiumStationScene.vue
│   │   ├── CesiumViewer.vue
│   │   └── CoverageReport.vue
│   ├── layout/              # 布局组件
│   │   └── MainLayout.vue
│   ├── router/              # 路由配置
│   │   └── index.js
│   ├── stores/              # 状态管理
│   │   └── user.js
│   ├── utils/               # 工具函数
│   │   └── request.js
│   ├── views/               # 页面视图
│   │   ├── Login.vue
│   │   ├── m03/
│   │   ├── m05/
│   │   └── m06/
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
├── vite.config.js           # Vite配置
└── package.json             # 依赖配置
```

## 三、路由设计

### 3.1 路由配置

```javascript
// router/index.js
const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Login.vue')
  },
  {
    path: '/',
    component: () => import('@/layout/MainLayout.vue'),
    redirect: '/m03/design',
    children: [
      {
        path: 'm03/project',
        name: 'M03Project',
        component: () => import('@/views/m03/ProjectList.vue'),
        meta: { title: '项目列表' }
      },
      {
        path: 'm03/design',
        name: 'M03Design',
        component: () => import('@/views/m03/Design.vue'),
        meta: { title: '三维场景设计' }
      },
      {
        path: 'm03/coverage',
        name: 'M03Coverage',
        component: () => import('@/views/m03/CoverageAssessment.vue'),
        meta: { title: '覆盖评估' }
      },
      {
        path: 'm03/station3d',
        name: 'Station3D',
        component: () => import('@/views/m03/Station3D.vue'),
        meta: { title: '基站环境' }
      }
    ]
  }
]
```

### 3.2 路由守卫

```javascript
// 路由守卫示例
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.path !== '/login' && !token) {
    next('/login')
  } else {
    next()
  }
})
```

## 四、组件设计

### 4.1 布局组件

**MainLayout.vue**：主布局组件
- 顶部导航栏
- 左侧菜单栏
- 主内容区域
- 响应式布局

### 4.2 业务组件

| 组件名 | 功能描述 | 依赖库 |
|--------|----------|--------|
| CesiumViewer | Cesium地图容器 | CesiumJS |
| CesiumStationScene | 基站环境场景 | CesiumJS |
| CesiumStationScene | 基站环境场景 | CesiumJS |
| CoverageReport | 覆盖评估报告 | ECharts |

### 4.3 组件规范

```vue
<!-- 组件模板规范 -->
<template>
  <div class="component-name">
    <!-- 组件内容 -->
  </div>
</template>

<script setup>
// 使用组合式API
import { ref, computed, onMounted } from 'vue'

// 组件属性
const props = defineProps({
  title: {
    type: String,
    default: ''
  }
})

// 组件事件
const emit = defineEmits(['update', 'delete'])

// 响应式数据
const data = ref(null)

// 计算属性
const computedData = computed(() => {
  return data.value
})

// 生命周期钩子
onMounted(() => {
  // 初始化操作
})
</script>

<style scoped>
/* 样式使用scoped避免污染 */
.component-name {
  /* 样式定义 */
}
</style>
```

## 五、状态管理

### 5.1 Pinia Store

```javascript
// stores/user.js
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useUserStore = defineStore('user', () => {
  const token = ref(localStorage.getItem('token') || '')
  const userInfo = ref(null)

  const setToken = (newToken) => {
    token.value = newToken
    localStorage.setItem('token', newToken)
  }

  const setUserInfo = (info) => {
    userInfo.value = info
  }

  const logout = () => {
    token.value = ''
    userInfo.value = null
    localStorage.removeItem('token')
  }

  return {
    token,
    userInfo,
    setToken,
    setUserInfo,
    logout
  }
})
```

## 六、API封装

### 6.1 请求封装

```javascript
// utils/request.js
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '@/router'

const service = axios.create({
  baseURL: '/api',
  timeout: 5000
})

// 请求拦截器
service.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers['Authorization'] = `Bearer ${token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
service.interceptors.response.use(
  response => {
    return response.data
  },
  error => {
    if (error.response?.status === 401) {
      ElMessage.error('登录已过期，请重新登录')
      router.push('/login')
    }
    return Promise.reject(error)
  }
)

export default service
```

### 6.2 API模块化

```javascript
// 按模块组织API
export const userAPI = {
  login: (data) => service.post('/m01/auth/login', data),
  logout: () => service.post('/m01/auth/logout'),
  getUserInfo: () => service.get('/m01/user/info')
}

export const projectAPI = {
  getList: () => service.get('/m05/project'),
  getDetail: (id) => service.get(`/m05/project/${id}`)
}
```

## 七、样式规范

### 7.1 CSS变量

```css
:root {
  --primary-color: #409eff;
  --success-color: #67c23a;
  --warning-color: #e6a23c;
  --danger-color: #f56c6c;
  --info-color: #909399;
}
```

### 7.2 命名规范

- 组件名：PascalCase (如：UserProfile)
- CSS类名：kebab-case (如：user-profile)
- 变量名：camelCase (如：userName)

## 八、环境配置

```javascript
// .env.example（实际项目中使用此文件）
VITE_API_M01=http://localhost:8080
VITE_API_M05=http://localhost:8085
```

> **说明**：前端通过 `vite.config.js` 中的 proxy 配置将 `/api/m01`、`/api/m03`、`/api/m05` 分别代理到对应后端服务，无需在前端代码中配置完整后端地址。

## 九、常用命令

| 命令 | 说明 |
|------|------|
| npm install | 安装依赖 |
| npm run dev | 启动开发服务器 |
| npm run build | 生产环境打包 |
| npm run preview | 预览打包结果 |
