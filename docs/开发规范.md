# 开发规范

**版本**: 1.1
**最后更新**: 2026-07-02
**适用对象**: 全体开发人员

---

## 变更记录

| 版本 | 日期 | 更新内容 | 维护人 |
|------|------|----------|--------|
| v1.0 | 2026-06-02 | 初始版本，定义Java/Vue/Python代码规范 | 人C |
| v1.1 | 2026-07-02 | 添加版本控制章节，统一数据库表命名规范 | 人C |

---

## 一、代码规范

### 1.1 Java代码规范

#### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 类名 | PascalCase | `UserController` |
| 方法名 | camelCase | `getUserById` |
| 变量名 | camelCase | `userName` |
| 常量 | UPPER_SNAKE | `MAX_COUNT` |
| 包名 | 全小写 | `com.comm.m05` |

#### 方法命名规范

```java
// 查询方法
getXxxById()           // 根据ID获取
listXxx()              // 获取列表
getXxxPage()           // 分页查询

// 新增方法
saveXxx()              // 保存
addXxx()               // 添加

// 更新方法
updateXxx()            // 更新

// 删除方法
deleteById()           // 根据ID删除
removeXxx()            // 移除
```

#### Controller规范

```java
@RestController
@RequestMapping("/m05/project")
public class ProjectController {
    
    @GetMapping
    public Result<List<Project>> list() {
        return Result.success(projectService.list());
    }
    
    @GetMapping("/{id}")
    public Result<Project> getById(@PathVariable Long id) {
        return Result.success(projectService.getById(id));
    }
    
    @PostMapping
    public Result<Boolean> save(@RequestBody Project project) {
        return Result.success(projectService.save(project));
    }
    
    @PutMapping("/{id}")
    public Result<Boolean> update(@PathVariable Long id, 
                                   @RequestBody Project project) {
        project.setId(id);
        return Result.success(projectService.updateById(project));
    }
    
    @DeleteMapping("/{id}")
    public Result<Boolean> delete(@PathVariable Long id) {
        return Result.success(projectService.removeById(id));
    }
}
```

### 1.2 Vue代码规范

#### 组件规范

```vue
<template>
  <div class="component-wrapper">
    <!-- 组件内容 -->
  </div>
</template>

<script setup>
// 1. 导入
import { ref, computed, onMounted } from 'vue'
import { useUserStore } from '@/stores/user'
import { ElMessage } from 'element-plus'

// 2. 定义属性
const props = defineProps({
  title: {
    type: String,
    required: true
  }
})

// 3. 定义事件
const emit = defineEmits(['update', 'delete'])

// 4. 响应式数据
const loading = ref(false)
const dataList = ref([])

// 5. 计算属性
const hasData = computed(() => dataList.value.length > 0)

// 6. 方法
const fetchData = async () => {
  loading.value = true
  try {
    // API调用
    dataList.value = []
  } catch (error) {
    ElMessage.error('获取数据失败')
  } finally {
    loading.value = false
  }
}

// 7. 生命周期
onMounted(() => {
  fetchData()
})
</script>

<style scoped>
.component-wrapper {
  padding: 20px;
}
</style>
```

#### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | PascalCase | `UserProfile.vue` |
| 组件名 | PascalCase | `UserProfile` |
| CSS类 | kebab-case | `.user-profile` |
| 事件名 | camelCase | `@update-data` |

## 二、Git规范

### 2.1 分支命名

| 分支类型 | 命名规范 | 示例 |
|----------|----------|------|
| 主分支 | main | main |
| 开发分支 | feature/xxx | feature/user-management |
| 修复分支 | fix/xxx | fix/login-bug |
| 发布分支 | release/v1.0.0 | release/v1.0.0 |

### 2.2 提交信息规范

```
<type>(<scope>): <subject>

feat: 添加用户登录功能
fix: 修复登录页面样式问题
docs: 更新README文档
style: 格式化代码
refactor: 重构用户认证模块
perf: 优化接口响应速度
test: 添加单元测试
```

### 2.3 提交示例

```bash
git add .
git commit -m "feat(m05): 添加项目管理模块

- 实现项目列表查询
- 实现项目详情查看
- 实现项目删除功能

Closes #123"
git push origin feature/project-management
```

## 三、接口规范

### 3.1 RESTful API设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /project | 获取项目列表 |
| GET | /project/{id} | 获取项目详情 |
| POST | /project | 创建项目 |
| PUT | /project/{id} | 更新项目 |
| DELETE | /project/{id} | 删除项目 |

### 3.2 响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    // 业务数据
  }
}
```

### 3.3 错误码规范

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

## 四、数据库规范

### 4.1 表命名规范

- 使用小写字母
- 多单词用下划线分隔
- 带模块前缀

```sql
-- 正确
m03_project
m03_device
m01_user

-- 错误
M05Project
Project
sys_user
```

### 4.2 字段命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 主键 | id | id |
| 外键 | xxx_id | user_id |
| 时间 | _time | create_time |
| 状态 | status | status |
| 布尔 | is_xxx | is_deleted |

### 4.3 SQL编写规范

```sql
-- 使用明确字段名，避免SELECT *
SELECT id, username, email, create_time
FROM m01_user
WHERE status = 1
ORDER BY create_time DESC
LIMIT 10;

-- 使用JOIN代替子查询
SELECT p.*, u.username
FROM m03_project p
INNER JOIN m01_user u ON p.creator_id = u.id;
```

## 五、文档规范

### 5.1 文件命名规范

```
{类型}-{主题}-{版本}.md
```

| 组成 | 说明 | 示例 |
|------|------|------|
| 类型 | 文档类型缩写 | API、DB、ARCH、SPEC、DEV、DEPLOY、ENV、INDEX |
| 主题 | 文档主题（中文） | 接口文档、数据库设计、架构设计 |
| 版本 | 可选，用于多版本文档 | v1.0、v2.1 |

### 5.2 类型缩写对照表

| 缩写 | 全称 | 说明 |
|------|------|------|
| API | API Document | API接口定义文档 |
| DB | Database | 数据库设计文档 |
| ARCH | Architecture | 架构设计文档 |
| SPEC | Specification | 详细规格设计文档 |
| DEV | Development | 开发规范文档 |
| DEPLOY | Deployment | 部署指南文档 |
| ENV | Environment | 环境依赖版本文档 |
| INDEX | Index | 文档索引目录 |
| OVERVIEW | Overview | 项目总概括文档 |
| PLAN | Plan | 实施计划文档 |

### 5.3 版本标识规则

版本号格式：`v{主版本号}.{次版本号}`

| 组成 | 说明 | 更新时机 |
|------|------|----------|
| 主版本号 | 重大变更 | 架构重构、文档结构调整、内容大幅重写 |
| 次版本号 | 增量更新 | 新增章节、修正错误、补充内容 |

每个文档必须在文件开头标注版本信息和变更记录。

### 5.4 文档内容模板

```markdown
# {文档标题}

**版本**: vX.X
**最后更新**: YYYY-MM-DD
**适用对象**: 目标读者
**文档用途**: 简要说明文档目的

---

## 变更记录

| 版本 | 日期 | 更新内容 | 维护人 |
|------|------|----------|--------|
| v1.0 | YYYY-MM-DD | 初始版本 | 人X |

---

## 目录

1. [章节一](#章节一)

---

## 正文内容

详细内容...

---

**文档结束**
```

### 5.5 代码注释

```java
/**
 * 根据ID获取用户信息
 *
 * @param id 用户ID
 * @return 用户信息
 * @author zhangsan
 * @date 2024-01-01
 */
public User getUserById(Long id) {
    return userMapper.selectById(id);
}
```

### 5.6 API文档

```markdown
### 获取项目列表

**请求**
```
GET /m05/project
```

**参数**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | int | 否 | 页码，默认1 |
| size | int | 否 | 每页条数，默认10 |

**响应**
```json
{
  "code": 200,
  "message": "success",
  "data": []
}
```
```

### 5.7 文档存放位置

| 位置 | 用途 |
|------|------|
| `docs/` | 核心文档（权威、常用） |
| `docs/子赛题规格设计/` | 各子赛题详细规格 |
| `docs/diagrams/` | 架构图、流程图资源 |
| `qgis-plugin/docs/` | QGIS插件专用文档 |

### 5.8 多人协作规范

| 规则 | 说明 |
|------|------|
| 版本控制 | 使用Git管理，每次修改必须更新版本号和变更记录 |
| 分支管理 | 文档修改在feature分支进行，合并后更新主文档 |
| 冲突处理 | 多人编辑同一文档时，先pull再push |
| 通知机制 | 重大变更需在团队群内通知 |

## 六、测试规范

### 6.1 单元测试

```java
@Test
public void testGetUserById() {
    User user = userService.getUserById(1L);
    assertNotNull(user);
    assertEquals("张三", user.getUsername());
}
```

### 6.2 接口测试

```bash
# 测试登录接口
curl -X POST http://localhost:8080/m01/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```

## 七、安全规范

### 7.1 敏感信息处理

```java
// 禁止在代码中硬编码密码
// 使用配置文件或环境变量

// 密码加密存储
BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
String encodedPassword = encoder.encode(rawPassword);
```

### 7.2 SQL注入防护

```java
// 使用参数化查询
@Select("SELECT * FROM user WHERE username = #{username}")
User findByUsername(@Param("username") String username);
```

### 7.3 XSS防护

```javascript
// 前端输入转义
const escapeHtml = (str) => {
  return str.replace(/[&<>"']/g, (match) => {
    const escapeMap = {
      '&': '&amp;',
      '<': '&lt;',
      '>': '&gt;',
      '"': '&quot;',
      "'": '&#39;'
    }
    return escapeMap[match]
  })
}
```

## 八、部署规范

### 8.1 环境分离

| 环境 | 用途 | 数据库 |
|------|------|--------|
| dev | 开发 | 开发库 |
| test | 测试 | 测试库 |
| prod | 生产 | 生产库 |

### 8.2 配置管理

```yaml
# application-dev.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/comm_platform_dev

# application-prod.yml
spring:
  datasource:
    url: jdbc:mysql://prod-server:3306/comm_platform
```
