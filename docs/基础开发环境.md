# 通信基建数智化全流程平台 – 环境依赖版本范围

> 本文档定义了开发、构建、运行本平台所需各组件的**最低版本**与**推荐版本**。团队成员应确保本地环境与 CI/CD 环境均在此范围内，避免因版本差异导致不可预知的问题。

---

## 一、基础运行环境

| 组件               | 最低版本 | 推荐版本            | 最高版本（已验证） | 备注                                           |
| ------------------ | -------- | ------------------- | ------------------ | ---------------------------------------------- |
| **JDK**            | 17       | 21 (LTS)            | 21                 | 不推荐高于 21，Spring Boot 3.2.x 官方支持至 21 |
| **Maven**          | 3.6.3    | 3.9.6+              | 3.9.x              | 需配置阿里云镜像加速                           |
| **Node.js**        | 18.0     | 20.11 LTS 或 22 LTS | 22.x               | Vue 3 & Vite 要求 Node 18+                     |
| **npm**            | 9.0      | 10.x                | 10.x               | 随 Node 自带，建议保持最新                     |
| **MySQL**          | 8.0.28   | 8.0.32+             | 8.0.x              | 必须启用 PostGIS 扩展                          |
| **Redis**          | 6.2      | 7.2                 | 7.2.x              | 用于缓存、会话、分布式锁                       |
| **Docker**         | 20.10    | 24.x+               | 最新稳定版         | 用于运行 MinIO, InfluxDB, EMQX                 |
| **Docker Compose** | 2.0      | 2.20+               | 最新稳定版         | 配合 docker-compose.yml                        |
| **Git**            | 2.20     | 2.40+               | 最新稳定版         | 版本控制                                       |

---

## 二、后端核心框架及库

> 以下依赖由 Maven 管理，通过 `pom.xml` 统一版本，开发人员无需手动安装。此处列出**最低兼容版本**与**推荐版本**。

| 框架/库                      | 最低版本 | 推荐版本 | 说明                                       |
| ---------------------------- | -------- | -------- | ------------------------------------------ |
| **Spring Boot**              | 3.2.0    | 3.2.5    | 基础框架                                   |
| **Spring Security**          | 6.2.0    | 6.2.4    | 认证授权                                   |
| **MyBatis Plus**             | 3.5.3    | 3.5.5    | ORM 增强                                   |
| **JJWT**                     | 0.11.5   | 0.12.6   | JWT 处理                                   |
| **MinIO Java SDK**           | 8.5.0    | 8.5.9    | 对象存储客户端                             |
| **InfluxDB Java Client**     | 6.9.0    | 7.0.0    | 时序数据库客户端                           |
| **Eclipse Paho MQTT Client** | 1.2.5    | 1.2.5    | MQTT 订阅发布                              |
| **Camunda (仅M04)**          | 7.20.0   | 7.22.0   | 工作流引擎                                 |
| **PostgreSQL JDBC Driver**   | 42.5.0   | 42.7.3   | PostGIS 连接（MySQL 使用 MySQL Connector） |
| **Lombok**                   | 1.18.26  | 1.18.32  | 简化代码                                   |

> 注：实际版本由父 pom 的 `<dependencyManagement>` 统一锁定，子模块继承即可。

---

## 三、前端核心框架及库

> 以下依赖由 npm/yarn 管理，通过 `package.json` 锁定版本。开发时需确保版本范围。

| 框架/库                | 最低版本 | 推荐版本 | 说明                   |
| ---------------------- | -------- | -------- | ---------------------- |
| **Vue**                | 3.4.0    | 3.5.13   | 核心框架               |
| **Vite**               | 5.0.0    | 5.4.11   | 构建工具               |
| **Element Plus**       | 2.6.0    | 2.8.8    | UI 组件库              |
| **Pinia**              | 2.1.0    | 2.2.6    | 状态管理               |
| **Vue Router**         | 4.2.0    | 4.4.5    | 路由（M06 门户使用）   |
| **Axios**              | 1.6.0    | 1.7.7    | HTTP 客户端            |
| **ECharts**            | 5.5.0    | 5.6.0    | 图表库                 |
| **Cesium**             | 1.115    | 1.124    | 三维地图（仅 M03/M05） |
| **vite-plugin-cesium** | 1.2.0    | 1.3.0    | Cesium Vite 插件       |
| **SCSS**               | 1.69.0   | 1.79.5   | CSS 预处理器           |

---

## 四、容器化依赖（Docker 镜像版本）

| 服务                  | 最低版本   | 推荐版本           | 镜像地址                 |
| --------------------- | ---------- | ------------------ | ------------------------ |
| **MySQL + PostGIS**   | 15-3.4     | 15-3.5             | `postgis/postgis:15-3.5` |
| **InfluxDB**          | 2.7.0      | 2.7.10             | `influxdb:2.7`           |
| **MinIO**             | 2024-01-01 | RELEASE.2025-04-24 | `minio/minio:latest`     |
| **EMQX**              | 5.3.0      | 5.8.4              | `emqx:5.8`               |
| **Redis（可选容器）** | 7.2.0      | 7.2.14             | `redis:7.2-alpine`       |

> 说明：若本地已安装 Redis 7.2，可不使用容器版本。

---

## 五、版本兼容性约束

### Java 与 Spring Boot
- Spring Boot 3.2.x **要求 JDK 17 或更高**，最高支持 21。
- 若使用 JDK 21，可开启虚拟线程特性（Spring Boot 3.2 + 已支持）。

### Node 与 Vite
- Vite 5 需要 Node.js 18+，推荐 20/22 LTS。
- 如果使用 Node 22，Vite 5 完全兼容。

### Cesium 与 Vue
- Cesium 1.115+ 需要现代浏览器，与 Vue 3 无冲突。
- `vite-plugin-cesium` 需要 Vite 5 和 Cesium 1.115+。

### MySQL 与 PostGIS
- MySQL 8.0 需安装 PostGIS 扩展（使用 `postgis/postgis` 镜像已集成）。
- 不支持 MySQL 5.7（JSON 功能与空间索引差异）。

---

## 六、升级策略与检查

1. **依赖升级审批**：任何核心依赖（Spring Boot、Vue、Cesium）的版本升级需团队评审。
2. **版本冻结期**：项目发布前 2 周，停止所有依赖大版本升级。
3. **自动化检查**：建议使用 `renovate` 或 `dependabot` 自动检测依赖更新，但需人工合并。

### 快速验证脚本（Windows PowerShell）

```powershell
# 检查关键工具版本
Write-Host "Java: " -NoNewline; java -version 2>&1 | Select-String "version"
Write-Host "Maven: " -NoNewline; mvn -v | Select-String "Apache Maven"
Write-Host "Node: " -NoNewline; node -v
Write-Host "npm: " -NoNewline; npm -v
Write-Host "MySQL: " -NoNewline; mysql --version
Write-Host "Redis: " -NoNewline; redis-cli --version
Write-Host "Git: " -NoNewline; git --version
Write-Host "Docker: " -NoNewline; docker --version
```

---

## 附录：推荐环境配置示例

### Windows 开发环境
- JDK 21.0.11 (Temurin)
- Maven 3.9.16 (阿里云镜像)
- Node.js 22.22 LTS
- MySQL 8.0.46 + PostGIS
- Redis 7.2.14 (本地服务)
- Docker Desktop 4.35 (WSL2 backend)
- IntelliJ IDEA 2024.3

### Linux / macOS 开发环境
- SDKMan 管理 Java/Maven
- nvm 管理 Node.js
- 使用 Docker 运行所有依赖（包括 Redis 容器）

---

**文档结束** – 请依据此范围统一团队环境，避免“在我电脑上能运行”的问题。