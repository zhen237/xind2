# 本地环境变量模板（团队共享默认，已提交到 git，成员可见）
# 用法（二选一）：
#   A. 直接用本默认：clone 后直接 bash deploy-s1.sh（脚本会自动读取本文件的 MYSQL_PWD）
#   B. 自定义密码：cp .env.example .env 并改成你的密码（.env 已被 gitignore，不会入库）
#
# deploy-s1.sh 读取优先级：shell 环境变量 MYSQL_PWD > 本地 .env > 本文件
# 注意：本文件含真实数据库密码与内部 API Key，仅适用于内部赛题私有仓库；请勿用于公网生产。
# ⚠️ 已执行密码轮换 (2026-07-30)：以下为轮换后新值；服务器 MySQL root 须同步执行 ALTER USER 改为此密码。
MYSQL_PWD=CHANGE_ME

# M03 内部接口（/api/m03/design/** 写删）API Key：QGIS 插件与内部服务调用时须携带 X-API-Key 头
M03_API_KEY=CHANGE_ME
